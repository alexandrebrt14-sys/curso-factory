# Triagem dos PRs abertos — 27/08/2026

## Método

Foram inspecionados os 16 pull requests abertos em `alexandrebrt14-sys/curso-factory`
(65, 62, 61, 58, 56, 52, 43, 42, 36, 35, 34, 28, 27, 26, 21, 1) apenas por comandos de
leitura: `gh pr view` (título, datas, rascunho, `mergeable`, `mergeStateStatus`, arquivos),
`git log origin/main -- <arquivo>`, `git show`, `git diff origin/main...origin/<branch>` e
comparação da lista de arquivos de cada branch contra a árvore de `origin/main`
(HEAD em `70a313e`, "Wave Agosto 2026", PR #69). O campo `mergeable` só foi obtido na
segunda consulta — na primeira o GitHub devolve `UNKNOWN` enquanto ainda calcula.
**Nada foi fechado, mergeado, rebaseado ou commitado.** Nenhum branch foi trocado.
A tabela abaixo é recomendação para decisão humana, não ação executada.

## Tabela

| PR | Título | Aberto em | Idade | Rascunho | mergeable / mergeStateStatus | Arquivos (n) | Recomendação | Evidência |
|---|---|---|---|---|---|---|---|---|
| 65 | chore(deps): Dependabot security-only (drop weekly version updates) | 2026-08-14 | 13 dias | não | MERGEABLE / BEHIND | 1 | mergear | Remove `.github/dependabot.yml` (47 linhas). O arquivo continua em `origin/main` com `interval: "weekly"`, inalterado desde `00f2747` (#29); o diff contra `main` aplica limpo, sem conflito. |
| 62 | fix(ci): concede pull-requests:read ao gitleaks | 2026-08-10 | 17 dias | não | MERGEABLE / BEHIND | 1 | mergear | O bloco `permissions:` de `.github/workflows/security-scan.yml` em `origin/main` tem só `contents: read` e `security-events: write` — `pull-requests: read` não entrou por nenhum outro caminho. Diff de 10 linhas, aplica limpo. |
| 61 | wiki: cinco lições do portal /educacao como premissa de fabricação | 2026-07-25 | 33 dias | não | CONFLICTING / DIRTY | 7 | rebase | O conflito é conteúdo duplicado: `docs/PLAYBOOK_CORRECAO_GEO_WORDPRESS_2026.md` já está em `origin/main` desde `2b93fac` (#63) e o diff entre as duas versões é **vazio** (idênticas). Já as 5 decisões (`wiki/decisions/acentuacao-ferramentas-e-armadilhas.md`, `arquivo-de-conteudo-sem-consumidor.md`, `integracao-conflitos-tem-dono.md`, `involucro-copiado-de-curso-irmao.md`, `numeros-de-curso-digitados-derivam.md`) **não existem** em `main`. Rebase descartando o playbook duplicado. |
| 58 | docs(geo): correções do conselho Gemini nas waves 22/22B | 2026-07-23 | 35 dias | não | MERGEABLE / BEHIND | 5 | mergear | Os 3 arquivos `raw/critica-gemini-22*.md` não existem em `origin/main` (a pasta tem `critica-gpt*.md`, mas nenhum Gemini). Só 8 commits atrás de `main` e aplica limpo sobre os CANONICAL introduzidos em `4f4db92` (#57). |
| 56 | ci: Bump actions/setup-python from 5 to 7 | 2026-07-20 | 38 dias | não | MERGEABLE / BEHIND | 2 | mergear | Dependabot verde e pequeno. Diff real contra `main`: `setup-python@v6 → v7` em duas linhas de `security-scan.yml` e `@v5 → v7` em `test.yml` (a única sobra em v5 no repositório). |
| 52 | ci: Bump actions/checkout from 6 to 7 | 2026-07-13 | 45 dias | não | MERGEABLE / BEHIND | 3 | mergear | Dependabot verde e pequeno. Diff real contra `main`: 5 ocorrências de `actions/checkout@v6 → v7` em `codeql.yml`, `security-scan.yml` e `test.yml`. Nenhuma já está em v7. |
| 43 | chore(codeowners): remove catch-all review request | 2026-06-30 | 58 dias | **sim** | MERGEABLE / BEHIND | 1 | manter | Aplica limpo: a linha `*  @alexandrebrt14-sys` ainda está em `origin/main:.github/CODEOWNERS` (introduzida em `00f2747`, #29). Mas é rascunho e afrouxa o review obrigatório de todo arquivo — decisão de política do dono, não limpeza mecânica. Manter aberto até a decisão; se aprovada, basta sair de rascunho. |
| 42 | docs: playbook de correção GEO em massa no WordPress (2026-06-29) | 2026-06-30 | 58 dias | não | MERGEABLE / BEHIND | 1 | fechar superado | O único arquivo do PR, `docs/PLAYBOOK_CORRECAO_GEO_WORDPRESS_2026.md`, já está em `origin/main` desde `2b93fac` (#63) e o `git diff` entre a versão de `main` e a do branch `docs/geo-wp-playbook-20260629` é **vazio** — conteúdo idêntico, o PR não acrescenta nada. |
| 36 | docs(geo): wave canônica 15-jun-2026 (GEO-SFE/AgentGEO, Share of Answer, AIO RCT) | 2026-06-15 | 73 dias | **sim** | CONFLICTING / DIRTY | 6 | rebase | Conteúdo ainda inédito: os 5 arquivos de `docs/research/geo-wave-junho-15-2026/` não existem em `origin/main`. O conflito vem só de `CLAUDE.md`, reescrito várias vezes desde junho (26 commits de `main` à frente, 1 commit próprio). Rebase resolvendo `CLAUDE.md` e sair de rascunho. |
| 35 | Aprimora padrão editorial e visual dos cursos | 2026-06-12 | 76 dias | **sim** | CONFLICTING / DIRTY | 20 | fechar superado | Todos os 20 arquivos já existem em `origin/main` e a mesma área foi reescrita depois por `2b93fac` "Diretriz editorial v3" (#63) e `e8e909c` "editorial guideline v4" (#64). `config/quality_rules.yaml` tem **315 linhas** em `main` contra **216** no branch, e `main` já cobre justificação de parágrafo (3 ocorrências no `quality_rules.yaml`, 19 em `src/templates/prompts/pt-br/draft.md`). Reaproveitar item a item se algo ainda faltar. |
| 34 | feat(remotion): abertura de curso em vídeo programático (player + render MP4) | 2026-06-09 | 79 dias | **sim** | CONFLICTING / DIRTY | 16 | rebase | Feature inédita: 11 dos 16 arquivos não existem em `main` (toda a pasta `remotion/` e `src/generators/video_generator.py` — `git ls-tree origin/main -- src/generators` lista só `tsx_generator`, `schema_builder`, `metadata_sync`, `build_validator`). Conflito nos 5 já existentes (`CLAUDE.md`, `cli.py`, `models.py`, `page.tsx.j2`, `tsx_generator.py`), com 26 commits de `main` à frente. |
| 28 | fix(template): renderer robusto + navegação + visuais ricos (stat/flow) | 2026-06-04 | 84 dias | **sim** | CONFLICTING / DIRTY | 8 | fechar superado | O coração do PR é `src/templates/page.tsx.j2`, que em `origin/main` tem **1382 linhas** após `404f4d4` "o contrato de geração passa a emitir bloco visual" (#67, de agosto) contra **1119 linhas** na versão do branch — `main` é mais nova e já traz bloco visual. Só a credencial "Nuvini/NVNI" segue inédita, e ela está isolada no PR 27. |
| 27 | fix(entity): credencial canônica inclui advisor de IA da Nuvini (NVNI) | 2026-06-03 | 85 dias | **sim** | CONFLICTING / DIRTY | 7 | rebase | Mudança ainda inédita: `git grep -i "nuvini\|NVNI"` em `origin/main` (`CLAUDE.md`, `README.md`, `config/clients/default/client.yaml`, `src/models.py`) não retorna **nenhuma** ocorrência. É uma linha por arquivo (campo `autor_credencial`), mas o branch está 28 commits atrás. Rebase — ou reaplicar a string à mão, é diff trivial. |
| 26 | feat(geo/seo): knowledge 2026 deep research (waves) | 2026-05-17 | 102 dias | **sim** | CONFLICTING / DIRTY | 183 | fechar superado | **182 dos 183** arquivos do PR já existem em `origin/main` — `docs/GEO_KNOWLEDGE_BASE_2026.md`, `GEO_OPERATING_SYSTEM.md`, `AI_DISCOVERY_STANDARDS_2026.md`, `SEO_KNOWLEDGE_BASE_2026.md`, `CONTRIBUTING.md`, `SECURITY.md` e `.github/*` entraram por `00f2747` (#29), que é ancestral de `main`. Único ausente: `src/unified_finops.py` (e `main` já tem `docs/FINOPS.md`). Rascunho de maio, 28 commits atrás. |
| 21 | feat: foundation and waves 6-10 | 2026-04-29 | 120 dias | **sim** | CONFLICTING / DIRTY | 99 | fechar superado | **98 dos 99** arquivos já existem em `origin/main` (`src/agentic/*`, `src/agents/tutor.py`, `src/agents/translator.py`, `examples/*`, `scripts/legacy/*` e a governança `.github/*`, todos por `00f2747`, #29). Único ausente: `src/unified_finops.py`. Rascunho de abril, CONFLICTING, 28 commits atrás. |
| 1 | docs(knowledge): add GEO/AEO/Agentic Commerce knowledge base | 2026-04-25 | 124 dias | **sim** | CONFLICTING / DIRTY | 20 | rebase | Apesar da idade, **17 dos 20** arquivos continuam ausentes de `main`: a pasta `docs/knowledge/geo-aeo/` inteira (`00-principios-mestres.md` … `50-fontes-e-links.md`) não existe em `origin/main` (`git ls-tree origin/main -- docs/knowledge` volta vazio) e o commit `eed7866` **não** é ancestral de `main`. Curiosamente `origin/main:.github/CODEOWNERS` já reserva a regra `/docs/knowledge/geo-aeo/`. Conflito só nos 3 arquivos comuns (`CLAUDE.md`, `README.md`, `pyproject.toml`). Avaliar se o conteúdo foi absorvido pelos `docs/GEO_*` consolidados antes de investir no rebase. |

## Resumo

- **mergear — 5 PRs:** #65, #62, #58, #56, #52. Todos `MERGEABLE`, pequenos, sem sobreposição com o que já está em `main`.
- **fechar superado — 5 PRs:** #42, #35, #28, #26, #21. Em todos, os arquivos já estão em `main` com conteúdo idêntico (#42) ou mais novo (#35, #28, #26, #21).
- **rebase — 5 PRs:** #61, #36, #34, #27, #1. Trazem conteúdo que comprovadamente ainda não existe em `main`, mas estão `CONFLICTING` porque a base andou de 21 a 28 commits.
- **manter — 1 PR:** #43. Aplica limpo, mas é rascunho e a mudança é decisão de política de review.

> Ordem sugerida: mergear primeiro os 5 verdes (#52 e #56 mexem nos mesmos workflows — um de cada vez), depois fechar os 5 superados e, só então, rebasear os 5 restantes, que exigem resolver `CLAUDE.md`.
