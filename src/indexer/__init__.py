"""Indexador de cursos para busca vetorial (Supabase + OpenAI embeddings).

`course_indexer.py` é um script standalone — execute via `python -m
src.indexer.course_indexer` ou importe `course_indexer.main()` em scripts
externos. Não está plugado no CLI principal.
"""
